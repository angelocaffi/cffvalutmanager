@page "/"
@inject SessionState Session
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() =>
        Navigation.NavigateTo(Session.IsUnlocked ? "/vault" : "/login");
}
