@page "/"
@inject SessionState Session
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() =>
        Navigation.NavigateTo(Session.IsUnlocked
            ? (Session.Role == "SuperAdmin" ? "/admin/tenants" : "/vault")
            : "/login");
}
